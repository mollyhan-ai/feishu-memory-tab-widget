{
  "name": "switchable-content-cards",
  "version": "0.2.0",
  "private": true,
  "description": "A data-driven switchable content card renderer and Codex Skill.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test"
  }
}
